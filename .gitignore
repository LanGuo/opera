# Virtual environments
.venv/

# Secrets
.env
.env.local

# Python cache
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Build artifacts
dist/
build/
*.egg-info/
*.egg

# Test cache
.pytest_cache/

# Eval run artifacts (can be large; host separately)
runs/

# Symlink to the private repo's internal docs (decisions log, research notes,
# bug logs) -- kept accessible here for convenience while working in this
# directory, but must never be tracked or pushed to the public repo.
docs-private
